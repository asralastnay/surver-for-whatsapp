# استخدام أحدث نسخة من WAHA
FROM devlikeapro/waha:latest

# ----------------------------------------------------------------
# 1. إعدادات المحرك
# ----------------------------------------------------------------
ENV WHATSAPP_DEFAULT_ENGINE=WEBJS

# ----------------------------------------------------------------
# 2. تثبيت المفاتيح وكلمات المرور (كما طلبت)
# ----------------------------------------------------------------
ENV WAHA_API_KEY=123456
ENV WAHA_DASHBOARD_USERNAME=admin
ENV WAHA_DASHBOARD_PASSWORD=123456

# ----------------------------------------------------------------
# 3. إعدادات الأداء القصوى (الحل السحري للسرعة) 🚀
# ----------------------------------------------------------------
# هذه الأسطر تجبر متصفح كروم على العمل بأقل استهلاك ممكن للذاكرة والمعالج
# --no-sandbox: ضروري جداً لبيئة Docker
# --disable-dev-shm-usage: يمنع امتلاء الذاكرة المؤقتة ويمنع الانهيار
# --disable-gpu: يعطل كارت الشاشة (غير موجود أصلاً) لتوفير الجهد
# --single-process: يدمج العمليات لتوفير الرام (خطير لكنه مفيد جداً في السيرفرات الصغيرة)
ENV WAHA_PUPPETEER_ARGS="--no-sandbox --disable-setuid-sandbox --disable-dev-shm-usage --disable-accelerated-2d-canvas --no-first-run --no-zygote --single-process --disable-gpu"

# التأكد من العمل في وضع "بدون واجهة" لتسريع الإقلاع
ENV WAHA_PUPPETEER_HEADLESS=True

# ----------------------------------------------------------------
# 4. إعدادات إضافية
# ----------------------------------------------------------------
ENV WAHA_PRINT_QR=True
ENV WAHA_APPS_ENABLED=False

# إعداد المنفذ
ENV PORT=3000
EXPOSE 3000
